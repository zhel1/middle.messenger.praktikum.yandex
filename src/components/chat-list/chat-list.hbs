<div class="chat-list">
    <div class="chat-list__header">
        {{> Input placeholder="Search for chat..." name="searh" }}
        {{> Button type="settings"}}
    </div>
    <ul class="chat-list__scroll">
        {{#each chat_list as |chat|}}
            {{> ChatItem chat=chat}}
        {{/each}}
    </ul>
</div>
